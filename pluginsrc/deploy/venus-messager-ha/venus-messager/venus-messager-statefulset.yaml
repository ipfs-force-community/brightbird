apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: venus-messager-{{.UniqueId}}
spec:
  replicas: {{.Replicas}}
  serviceName: venus-messager-{{.UniqueId}}-svc
  selector:
    matchLabels:
      app: venus-messager-{{.UniqueId}}-pod
  template:
    metadata:
      labels:
        app: venus-messager-{{.UniqueId}}-pod
    spec:
      initContainers:
        - name: init-myservice
          image: {{if gt (len .PrivateRegistry) 0}}{{.PrivateRegistry}}/{{end}}library/busybox:1.35
          command: ['sh', '-c', "mkdir -p /root/.venus-messager; cp /root/config.tmp /root/.venus-messager/config.toml"]
          volumeMounts:
            - mountPath: /root
              name: venus-messager-pvc
            - name: venus-messager-cfg
              mountPath: "/root/config.tmp"
              subPath: "config.toml"
      containers:
        - name: venus-messager-{{.UniqueId}}
          image: "{{if gt (len .PrivateRegistry) 0}}{{.PrivateRegistry}}/{{end}}filvenus/venus-messager:{{.CodeVersion}}"
          args: ["run"]
          ports:
            - containerPort: 39812
              protocol: TCP
          volumeMounts:
            - mountPath: /root
              name: venus-messager-pvc
            - name: venus-messager-cfg
              mountPath: "/root/config.tmp"
              subPath: "config.toml"
      volumes:
        - name: venus-messager-cfg
          configMap:
            name: venus-messager-{{.UniqueId}}
            items:
              - key: config.toml
                path: config.toml   
  volumeClaimTemplates:
    - metadata:
        name: venus-messager-pvc # name+podname
        labels:
          app: venus-messager-{{.UniqueId}}-pvc
      spec:
        accessModes: [ "ReadWriteOnce" ]
        storageClassName: "nfs-client"
        resources:
          requests:
            storage: 1Mi