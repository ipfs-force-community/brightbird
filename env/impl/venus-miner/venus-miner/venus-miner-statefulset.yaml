apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: venus-miner-{{.UniqueId}}
spec:
  replicas: 1
  serviceName: venus-miner-{{.UniqueId}}-svc
  selector:
    matchLabels:
      app: venus-miner-{{.UniqueId}}-pod
  template:
    metadata:
      labels:
        app: venus-miner-{{.UniqueId}}-pod
    spec:
      containers:
        - name: venus-miner-{{.UniqueId}}
          image: "{{if gt (len .PrivateRegistry) 0}}{{.PrivateRegistry}}/{{end}}filvenus/venus-miner:{{.CodeVersion}}"
          args: ["run"]
          ports:
            - containerPort: 12308
              protocol: TCP
          volumeMounts:
            - mountPath: /root
              name: venus-miner-pvc
            - name: venus-miner-cfg
              mountPath: "/root/.venusminer/config.toml"
              subPath: "config.toml"
      volumes:
        - name: venus-miner-cfg
          configMap:
            name: venus-miner-cfg-{{.UniqueId}}
            items:
              - key: config.toml
                path: config.toml
  volumeClaimTemplates:
    - metadata:
        name: venus-miner-pvc # name+podname
        labels:
          app: venus-miner-{{.UniqueId}}-pvc
      spec:
        accessModes: [ "ReadWriteOnce" ]
        storageClassName: "nfs-client"
        resources:
          requests:
            storage: 100Mi