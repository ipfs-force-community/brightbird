apiVersion: v1
kind: Service
metadata:
  name: venus-miner-{{.UniqueId}}-svc
spec:
  clusterIP: None
  selector:
    app: venus-miner-{{.UniqueId}}-pod
  ports:
    - protocol: TCP
      port: 12308
      name: api

